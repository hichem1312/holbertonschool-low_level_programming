#include "main.h"
#include <string.h>
/**
 * _strlen - writes the character c to stdout
 * @s:d
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int _strlen(char *s)
{
int lengt;
lengt = strlen(s);
return (lengt);
}
